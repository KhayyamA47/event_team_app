import 'package:event_team_app/pages/main/home/components/next_events/components/header.dart';
import 'package:flutter/cupertino.dart';

class NextEvent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        NextEventsHeader()
      ],
    );
  }
}
